server {
        listen 80;
        listen [::]:80;

        root /var/www/ubuntu/{{ site_name }};
        index index.html index.htm index.nginx-debian.html;

        server_name {{ site_name }}  www.{{ site_name }};

        location / {
                try_files $uri $uri/ =404;
        }
}
